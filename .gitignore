# Keep versioned snapshots out of the main tree — move them to versions/ if you want to archive them
NewGen_Team_Interactive_v*.html

# Local config — contains GitHub PAT, never commit
config.local.js
